name: 🚀 CI & Deploy

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]
  workflow_dispatch:

jobs:
  test:
    name: 🧪 Tests
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      # Lint informatif (non bloquant) : le codebase a des erreurs lint
      # héritées. À rendre bloquant une fois nettoyé.
      - name: 🔍 Lint (non bloquant)
        run: npm run lint
        continue-on-error: true

      # Gate dur : typecheck + tests doivent passer pour déployer.
      - name: 🧠 Typecheck
        run: npm run typecheck

      - name: ✅ Unit tests
        run: npm test

  deploy:
    name: 🚀 Deploy
    needs: test
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      # Le CLI Vercel lit automatiquement VERCEL_TOKEN depuis l'env.
      VERCEL_TOKEN: ${{ secrets.COLL_VERCEL_TOKEN }}
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🚀 Deploy to Vercel (production)
        if: github.ref == 'refs/heads/main'
        run: npx vercel --prod --yes

      - name: 🔎 Deploy to Vercel (preview)
        if: github.ref == 'refs/heads/staging'
        run: npx vercel --yes
